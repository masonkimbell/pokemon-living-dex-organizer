[tool.poetry]
name = "pokemon-living-dex-organizer"
version = "0.1.0"
description = ""
authors = ["masonkimbell <mkimbell1@outlook.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
pillow = "^11.0.0"
python-dotenv = "^1.0.1"

[tool.poetry.group.dev.dependencies]
black = "^24.10.0"
mypy = "^1.12.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
